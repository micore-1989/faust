# ─── Core agent (required on all hosts) ────────────────────────────
httpx>=0.27           # async HTTP for OpenAI-compatible backends
aiohttp>=3.9          # async web server + WebSocket for touchscreen UI
pyyaml>=6.0           # SKILL.md frontmatter
pydantic>=2.0         # typed tool-input validation

# ─── Dev / testing (all hosts) ─────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.23
